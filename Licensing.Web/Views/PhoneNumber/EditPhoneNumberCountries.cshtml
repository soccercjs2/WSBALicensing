@model Licensing.Business.ViewModels.PhoneNumberCountriesVM

@{
    ViewBag.Title = "Edit Phone Number Countries";
}

<h2>Edit Phone Number Countries</h2>

@using (Html.BeginForm("Edit", "PhoneNumberCountry", FormMethod.Post))
{
    <hr />

    <div class="row">
        @Html.ActionLink("Return to Admin Dashboard", "Index", "Admin", null, new { @class = "btn btn-primary" })
        <input class="btn btn-success" type="submit" value="Sync" />
    </div>

    <hr />

    <div class="row">
        <div class="col-sm-4 dashboard-column">
            <h3><u>Active Codes</u></h3>
            @if (Model.Codes != null && Model.Codes.Count > 0)
            {
                <table class="standard-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Code</th>
                            <th>Number</th>
                        </tr>
                    </thead>
                    @for (int i = 0; i < Model.Codes.Count; i++)
                    {
                        @Html.HiddenFor(m => Model.Codes[i].PhoneNumberCountryId)
                        @Html.HiddenFor(m => Model.Codes[i].Name)
                        @Html.HiddenFor(m => Model.Codes[i].CountryCode)
                        @Html.HiddenFor(m => Model.Codes[i].InternationalCode)
                        @Html.HiddenFor(m => Model.Codes[i].Active)

                        <tr>
                            <td>@Model.Codes[i].Name</td>
                            <td>@Model.Codes[i].CountryCode</td>
                            <td>@Model.Codes[i].InternationalCode</td>
                        </tr>
                    }
                </table>
            }
            else
            {
                <span>No codes.</span>
            }
        </div>
        <div class="col-sm-4 dashboard-column">
            <h3><u>Codes to be Added</u></h3>
            @if (Model.CodesToBeAdded != null && Model.CodesToBeAdded.Count > 0)
            {
                <table class="standard-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Code</th>
                            <th>Number</th>
                        </tr>
                    </thead>
                    @for (int i = 0; i < Model.CodesToBeAdded.Count; i++)
                    {
                        @Html.HiddenFor(m => Model.CodesToBeAdded[i].PhoneNumberCountryId)
                        @Html.HiddenFor(m => Model.CodesToBeAdded[i].Name)
                        @Html.HiddenFor(m => Model.CodesToBeAdded[i].CountryCode)
                        @Html.HiddenFor(m => Model.CodesToBeAdded[i].InternationalCode)
                        @Html.HiddenFor(m => Model.CodesToBeAdded[i].Active)

                        <tr>
                            <td>@Model.CodesToBeAdded[i].Name</td>
                            <td>@Model.CodesToBeAdded[i].CountryCode</td>
                            <td>@Model.CodesToBeAdded[i].InternationalCode</td>
                        </tr>
                    }
                </table>
            }
            else
            {
                <span>No codes will be added.</span>
            }

            <h3><u>Codes to be Activated</u></h3>
            @if (Model.CodesToBeActivated != null && Model.CodesToBeActivated.Count > 0)
            {
                <table class="standard-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Code</th>
                            <th>Number</th>
                        </tr>
                    </thead>
                    @for (int i = 0; i < Model.CodesToBeActivated.Count; i++)
                    {
                        @Html.HiddenFor(m => Model.CodesToBeActivated[i].PhoneNumberCountryId)
                        @Html.HiddenFor(m => Model.CodesToBeActivated[i].Name)
                        @Html.HiddenFor(m => Model.CodesToBeActivated[i].CountryCode)
                        @Html.HiddenFor(m => Model.CodesToBeActivated[i].InternationalCode)
                        @Html.HiddenFor(m => Model.CodesToBeActivated[i].Active)

                        <tr>
                            <td>@Model.CodesToBeActivated[i].Name</td>
                            <td>@Model.CodesToBeActivated[i].CountryCode</td>
                            <td>@Model.CodesToBeActivated[i].InternationalCode</td>
                        </tr>
                    }
                </table>
            }
            else
            {
                <span>No codes will be activated.</span>
            }

            <h3><u>Codes to be Changed</u></h3>
            @if (Model.CodesToBeChanged != null && Model.CodesToBeChanged.Count > 0)
            {
                <table class="standard-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Code</th>
                            <th>Number</th>
                        </tr>
                    </thead>
                    @for (int i = 0; i < Model.CodesToBeChanged.Count; i++)
                    {
                        @Html.HiddenFor(m => Model.CodesToBeChanged[i].PhoneNumberCountryId)
                        @Html.HiddenFor(m => Model.CodesToBeChanged[i].Name)
                        @Html.HiddenFor(m => Model.CodesToBeChanged[i].CountryCode)
                        @Html.HiddenFor(m => Model.CodesToBeChanged[i].InternationalCode)
                        @Html.HiddenFor(m => Model.CodesToBeChanged[i].Active)

                        <tr>
                            <td>@Model.CodesToBeChanged[i].Name</td>
                            <td>@Model.CodesToBeChanged[i].CountryCode</td>
                            <td>@Model.CodesToBeChanged[i].InternationalCode</td>
                        </tr>
                    }
                </table>
            }
            else
            {
                <span>No codes will be changed.</span>
            }
        </div>
        <div class="col-sm-4 dashboard-column">
            <h3><u>Codes to be Deactivated</u></h3>
            @if (Model.CodesToBeDeactivated != null && Model.CodesToBeDeactivated.Count > 0)
            {
                <table class="standard-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Code</th>
                            <th>Number</th>
                        </tr>
                    </thead>
                    @for (int i = 0; i < Model.CodesToBeDeactivated.Count; i++)
                    {
                        @Html.HiddenFor(m => Model.CodesToBeDeactivated[i].PhoneNumberCountryId)
                        @Html.HiddenFor(m => Model.CodesToBeDeactivated[i].Name)
                        @Html.HiddenFor(m => Model.CodesToBeDeactivated[i].CountryCode)
                        @Html.HiddenFor(m => Model.CodesToBeDeactivated[i].InternationalCode)
                        @Html.HiddenFor(m => Model.CodesToBeDeactivated[i].Active)

                        <tr>
                            <td>@Model.CodesToBeDeactivated[i].Name</td>
                            <td>@Model.CodesToBeDeactivated[i].CountryCode</td>
                            <td>@Model.CodesToBeDeactivated[i].InternationalCode</td>
                        </tr>
                    }
                </table>
            }
            else
            {
                <span>No codes will be deactivated.</span>
            }

            <h3><u>Codes to be Deleted</u></h3>
            @if (Model.CodesToBeDeleted != null && Model.CodesToBeDeleted.Count > 0)
            {
                <table class="standard-table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Code</th>
                            <th>Number</th>
                        </tr>
                    </thead>
                    @for (int i = 0; i < Model.CodesToBeDeleted.Count; i++)
                    {
                        @Html.HiddenFor(m => Model.CodesToBeDeleted[i].PhoneNumberCountryId)
                        @Html.HiddenFor(m => Model.CodesToBeDeleted[i].Name)
                        @Html.HiddenFor(m => Model.CodesToBeDeleted[i].CountryCode)
                        @Html.HiddenFor(m => Model.CodesToBeDeleted[i].InternationalCode)
                        @Html.HiddenFor(m => Model.CodesToBeDeleted[i].Active)

                        <tr>
                            <td>@Model.CodesToBeDeleted[i].Name</td>
                            <td>@Model.CodesToBeDeleted[i].CountryCode</td>
                            <td>@Model.CodesToBeDeleted[i].InternationalCode</td>
                        </tr>
                    }
                </table>
            }
            else
            {
                <span>No codes will be deleted.</span>
            }
        </div>
    </div>
}